using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Input;

namespace DesktopBattle
{
    class Pistol : Gun
    {
        /// <summary>
        /// Creates a new bullet object on screen.
        /// </summary>
        public override void Shoot(MouseState aCurrentMouseState)
        {
            /*
            oldMouse = aCurrentMouseState;
            Player = mHero.Position;
            Mouse = new Vector2(oldMouse.X, oldMouse.Y);
            */
            //make code to generate a new bullet
        }
    }
}
